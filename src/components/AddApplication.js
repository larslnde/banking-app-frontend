import React from 'react';
import { withRouter } from 'react-router';

const AddApplication = () => (
    <div>
        <button onClick={console.log('AddApplication rtiggered')}>New Application</button>
    </div>
);

export default AddApplication;